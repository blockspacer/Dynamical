#include "main_render.h"

MainRender::MainRender(Instance& instance, Device& device, Swapchain& swap) : renderpass(device, swap), instance(instance), device(device), swap(swap) {
    
}

void MainRender::render() {
    
}

MainRender::~MainRender() {
    
}
